# Copy to .env and adjust. config/env.js validates these at boot.
NODE_ENV=development
PORT=5000

# Single allowed origin - never "*". Matches the Vite dev server.
CLIENT_ORIGIN=http://localhost:5173
