# Prompt 04 — Backend Implementation

Implement the minimal Express backend required by the frontend.

Architecture:

routes
→ controllers
→ services
→ models

Implement:

GET /api/listings/:id

Use structured error handling and validation.

Keep data local/static unless a database is genuinely required.

Do not over-engineer the backend.